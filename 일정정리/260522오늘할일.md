# LLM GPT 구현 목적

## 라이브러리 없이 구현을 목적

## numpy (수치해석 라이브러리)

## matplotlib (그래프 그려주는 라이브러리)

```
colab -> 파이썬 버전 중요
최신버전의 라이브러리는 지원하지 않는 경우가 있음

ds -> Anaconda

intractive python notebook(확장파일만 다르고 ipy라는 툴만 설치하면 쓸 수 있음)
ipynb 확장명을 가지면, 한줄씩 실행->결과
```

- 3일, 4일로 기간을 짧게 잡는 이유
```
궁극적으로 해야하는건 LLM이니깐 LLM으로 넘어가야한다.
```

## Perceptron
```
뉴런을 모방해서 컴퓨터로 만든것

이걸 2개 3개 늘리니 컴퓨터로 뉴런을 모방하는 게 가능해졌다.

추론 - 그게 가능해?
```

# LLM

## Neural Network
- 다음 단어를 확률적으로 알려주는 Neural Network
- 쿼리해서 문장을 만들어주는 것 - LLM의 진실

## 입력층
- 물어보는 것 또는 단어들에 대해 그 다음 단어 하나를 순서 %로 알려줌

## CNN
- 이미지 인식

## RNN
- Language Model

## 첫번째 과제
```
Loss가 언제 많이 떨어지는지, 이런 관점으로 접근
```

## LLM
```
책내용은 토크나이저 이쪽이 부실 (이미 있는 라이브러리 사용) - BPE 기반이 아님
임베딩, 모델 구현은 책이 괜찮음
```