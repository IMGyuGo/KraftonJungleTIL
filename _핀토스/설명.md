#

```
bios -> boot loader

boot loader
-> os source memory upload

cpu
-> kernel
```

- QEMU software
  - remote debugging으로 개발 가능
    - 어떤 CPU로 emulaotr simulator? (x86 32, 64bit 다 지원)

```
cpu가 한 프로그램을 실행한다면? OS가 하는 일은 무엇일까?
- 메모리에다가 OS가 올려줘야 한다. (시작점을 cpu한테 알려줘야 한다.)
p2, p3 (어떻게 여러프로그램이) 한번에 실행되는 것처럼 보일까?
- 방법 1 : p1 실행후 p2를 실행 -> 이 방법은 아닌 것 같다.
- 방법 2 : 한 줄씩 실행하도록 한다. -> 이 방법이 실제로 되려면 다른 방법으로 해야 함.
    - time slicing (process scheduling - thread scheduling)
        - hardware 가 clock을 하나 만들어둬서 (100만분의 1초에 한번씩 signal) cpu는 시그널이 올 때마다 다른 녀석을 찾음 (timer handler - OS에서 작동)
            - clock이 올 때 cpu가 timer로 넘겨줬을 때 다음 작업을 timer handler가 바꿔서 보내주면 된다.
            - alarm clock -> timer sleep 함수 구현
                - 지금 sleep list는 while문으로 되어 있어 현재 틱이 n 틱보다 안되어 있으면 thread_yield()
            - scheduler보다 handler의 바꾸는 작업이 더 느리다면? 어떻게 될까?
                - context switching total 시간이 너무 길면 스위칭이 일어나기 전제 스위칭해라 계속 이런 이상한 루프 때문에 timer 시간이 중요하다.
                - interrupt disable 기능으 잘 사용해야 한다.
    - 1. timer sleep 잘 구현
    - 2. creation 될 떄 sorted list로 priority를 넣어줌 (why? -> 계속 돌릴때마다 우선순위를 찾으면 오버헤드가 발생)을 구현
    - 3. priority Inversion (MLFQS - Multi Level Frequency Queue System) 이것을 구현

- priority Inversion (MLFQS - Multi Level Frequency Queue System)
    - 화성 탐사선 보낼 때 이것 때문에 다시 되돌린 적이 있다고 함
    -


    -
```
