# 2026-04-25 Meeting Contents

## 주요 안건

### 1. 코어 타임 시간

`오후 9시 25분 ~ 오후 10시 30분`

### 진행 계획

`1topic 1quiz`

- 진행 순서
  - `박지용` 님
    - [youtube] kaist lecture
    ```
    ready_list
    - 실행 가능한 스레드 큐
    - CPU 스케줄링 대상
    sleep_list
    - 시간이 안 돼서 못 깨어나는 스레드
    - timer interrupt로 깨움
    ```
    ```
    상태 전이
    RUNNING -> BLOCKED (sleep_list)
    -> (timer interrupt) -> READY (ready_list)
    ```
  - `김규민` 님

  - `박승현` 님
  - `박건우` 님

```

```

- 9시 25분 시작
- 10시 30분 끝

## 박지용

```
wait - [lock], [sleep], [wake-lock]
signal - []
broadcast
```

왜 acquire을 재귀로 사용할 수 없나요?

## 박승현

```
뭐 하기 전에 인터럽트 해야 한다.

인터럽트를 오래 꺼두면 발생할 수 있는 문제?
```

## 박건우

```
thread에
block이랑 ready? 왜 나누지

block -> 조건 unblock()

1. sleep list 와 ready list 같이 있으면 어떨까?
->
```

```
malloc의 힙리스트와 free list
-> allocator
-> free

구조가 너무 닮아 있다.
thread pool이니까 닮아있고
죽인다.

tick이 쌓인다.
interrupt가 꺼두면 주먹을

interrupt를 꺼두면 -> 어떤 코드를 꺼두는 걸까?
-> 이렇게 꺼두면 다른 키보드나 마우스 입력이 인식이 되지 않는다.
-> 코드로 임의로 꺼둘 수 있나?
    -> 꺼둘 수 있을 수 있다.
    -> 쓰레드가 안 들어갈거라 했는데,
    -> 주먹이 안된다는거는 인터럽트 밑에 쓰레드 틱이 발생이 안됨
    -> 위에 틱도 안되고 밑에 틱도 일을 했는지 안했는지 작동이 안되니
    -> time slice가 안되고 CPU가 없어진다.
    -> 풀리는 순간

타이머 인터럽트가 발생하면 -> 얼음(잠깐 시간이 멈춘다.)
인터럽트가 꺼져있는 상태에서는 쓰레드가 일을 못받으니 한번에 쌓인다.
후루룩 -> 밀린 인터럽트가 후루룩 발생
```
